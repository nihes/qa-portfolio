{
  "name": "qa-portfolio-mobile-web",
  "version": "1.0.0",
  "description": "Playwright mobile device emulation suite (viewport, UA, touch) for saucedemo.com",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "report": "playwright show-report",
    "install:browsers": "playwright install chromium webkit"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "typescript": "^5.6.0"
  }
}
