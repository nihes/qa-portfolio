{
  "name": "qa-portfolio-api-graphql",
  "version": "1.0.0",
  "private": true,
  "description": "GraphQL API tests (Mocha + Chai + axios) against the public Countries GraphQL API (countries.trevorblades.com) — queries, variables, error handling and schema validation.",
  "scripts": {
    "test": "mocha"
  },
  "devDependencies": {
    "axios": "^1.7.7",
    "chai": "^4.5.0",
    "mocha": "^10.7.3"
  }
}
